# 연도별 totalTest

---

### 2022.04.24 test

1과목 75점

2과목 80점

3과목 60점

4과목 95점

5과목 80점

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/e53ffbfd-c337-4df2-8f5d-11ada5da36d6/Untitled.png)

---

```cpp
3번 익스트림 프로그래밍에 대해 틀린 것
```

- 대표적 구조적 방법론 중 하나이다.

“익스트림 프로그래밍은 대표적 `애자일` 방법론 중 하나이다”

익스트림 프로그래밍은 소규모 조직이 불확실하고 변경이 많은 요구를 접했을 때 행하는 방법이며, 개발 문서 보다는 소스 코드에 중점을 둔다.

```cpp
5번 요구분석 사항에서 비기능적 요구에 대해 옳은 것
```

- 차량 대여 시스템이 제공하는 모든 화면이 3초 이내에 사용자에게 보여야 한다는 것은 비기능적 요구이다.

기능적 : 조회, 인출, 입금 등 실제 `필요한 기능적 동작`

비기능적 : 보안, 품질, 안정화, 추가사항 등 실제 `수행에 보조적인 요구사항`

```cpp
15번 GoF 디자인 패턴을 생성, 구조, 행동 패턴의
세 그룹으로 분류할 때 구조 패턴이 아닌 것
```

- Builder 패턴

구조패턴 : 어댑터, 브릿지, 컴포넌트, 데코레이터, 퍼싸트, 플라이웨이트, 프록시

“구어브컴프폴퍼데” = “구워봐컴포폴포대”

생성패턴 : 추상 팩토리, 빌더, 팩토리 메소드, 프로토타입, 싱글톤

“생추빌팩프싱” = “상추밭싱싱팩폭”

행위패턴 : 역할 사슬, 커맨드, 인터프리터, 이터레이터, 미디에이터, 메멘토, 옵저버, 상태, 전략, 템플릿 메소드, 비지터

```cpp
18번 소프트웨어 모델링과 관련한 설명으로 틀린 것
```

- 모델링 작업의 결과물은 다른 모델링 작업에 영향을 줄 수 없다.

모델링 작업의 결과물은 다른 모델링 작업에 영향을 줄 수 있다.

```cpp
19번 유스케이스 다이어그램에 관련된 내용으로 틀린 것
```

- 시스템과 상호작용하는 외부시스템은 액터로 파악해서는 안된다.

액터는 시스템과 상호작용하는 모든것을 뜻하기 때문에 시스템이 예외가 되지 않는다.

```cpp
25번 테스트 케이스와 관련한 설명으로 틀린 것
```

- 테스트 목표 및 테스트 방법을 결정하기 전에 테스트 케이스를 작성해야 한다.

테스트 케이스 실행이 통과되었는지 실패하였는지 판단하기 위한 기준을 테스트 오라클이라고 하는게 맞다.

```cpp
30번 화이트박스 테스트와 관련해 틀린 것
```

- 프로그램 구조를 고려하지 않기 때문에 테스트 케이스는 프로그램 또는 모듈의 요구나 명세를 기초로 결정한다.

테스트 데이터를 이용해 실제 프로그램을 실행함으로써 오류를 찾는 동적테스트라는 지문은 맞는 지문.

```cpp
33번 인스펙션 과정
```

- 계획 - 사전교육 - 준비 - 익스펙션 회의 - 수정 - 후속조치

```cpp
40번 단위 테스트 도구로 사용될 수 없는 것
```

- lgpUnit

CppUnit, JUnit, HttpUnit 등은 실제 사용하는 테스트 도구

```cpp
41번 키의 부분 집합이 결정자가 되는 부분 종속이 존재하지 않는 정규형
```

- BCNF

결정자가 어쩌고 하면 BCNF

```cpp
46번 물리적 데이터베이스 구조의 기본 데이터 단위인 저장
레코드의 양식을 설계할 때 고려사항이 아닌 것
```

- 트랜잭션 모델링

저장 레코드 양식 설계시 고려사항은 데이터 타입, 데이터 값의 분포, 접근 빈도 등이 있다.

트랜잭션 모델링은 개념적 설계 단계에서 행한다.

```cpp
47번 REVOKE문과 같이 데이터의 사용권한을 관리하는데 사용하는 언어
```

- DCL(Data Control Language)

DCL 종류

commit : 명령어 수행 결과를 물리적 디스크로 저장, 명령어 수행 성공을 알림

rollback : 명령어 수행 실패를 알리고 수행 결과를 원복

grant : 데이터베이스 사용자에게 사용 권한 부여

revoke : 데이터베이스 사용자로부터 사용 권한 취소

```cpp
53번 키의 종류 중 유일성과 최소성을 만족하는 속성 또는 속성들의 집합
```

- Candidate Key

```cpp
54번 데이터 베이스 개념적 설계 단계에 대한 설명으로 틀린 것
```

- 트랜잭션 인터페이스를 설계 및 작성한다.

개념적 설계 : 트랜잭션 모델링, E-R다이어그램

논리적 설계 : 트랜잭션 인터페이스, 스키마 평가 및 정제

```cpp
56번 데이터 구조에 따라 개념 세계나 컴퓨터 세계에서 실제로 표현된 값을 처리하는 작업
```

- Operation

구조 : 논리적 개체 타입들 간 관계, 데이터 구조 및 정적 성질 표현

연산 : 실제 데이터 처리 작업에 대한 명세, 조작하는 기본 도구

제약 조건 : 실제 데이터의 논리적인 제약 조건

```cpp
58번 무결성 제약조건 중 개체 무결성 제약조건에 대해 옳은 것
```

- 기본키는 NULL 값을 가져서는 안되며 릴레이션 내에 오직 하나의 값만 존재해야 한다.

```cpp
70번 파이썬에서 순서있고 불변
```

- 튜플

리스트 - 순서있고 가변,

튜플 - 순서있고 불변,

셋 - 순서없고, 중복x

사전 - 순서없고 key value

```cpp
82번 접속인가 여부를 점검해서 인가된 경우 접속 허용, 그 반대 경우 거부 유틸리티
```

- tcp wrapper

```cpp
95번 접근 통제 방법 중 조직 내 직무, 직책 등 개인의 역할에 따라 부여하는 접근 정책
```

- RBAC(Role Based Access Control)

직무, 직책 등 - Role Based

미리 정해진 정책과 보안 등급 - MAC(Mandatory Access Control)

사용자 또는 신분 - DAC(Discretionary Access Control)

```cpp
97번 각 사용자 인증 유형에 대한 설명으로 적절치 않은 것
```

- 존재 : 주체는 ‘그를 대체하는 것’을 보여주며 패턴, QR 등이 있다.

존재는 본인을 나타내는 것을 뜻한다. 홍채, 지문 등

지식 - 본인이 알고 있는 것. 패스워드, PIN

소유 - 본인이 가지고 있는 것. 토큰, 스마트카드

존재 - 본인을 나타내는 것. 홍채, 지문

행위 - 본인이 하는 것. 서명, 움직임, 음성

```cpp
99번 기존 라우터, 스위치 등과 같이 하드웨어에 의존하는 네트워크 체계에서
안정성, 속도, 보안 등을 소프트웨어로 제어, 관리하기 위해 개발됨
```

- SDN(Software Defined networking)

문제처럼 소프트웨어를 통해서 제어, 관리 한다는 키워드가 나오면 SDN이라고 추측할 수 있다.

NFS(Network File System) : 네트워크 상에서 공유되는 파일시스템, 다른 원격 호스트의 파일 시스템을 로컬 디스크에 접근하듯 간단하게 접근해 자신의 디렉토리처럼 사용가능

Network Mapper : 네트워크 보안 유틸리티, 네트워크 보안을 위해 물리적 연결과 어떤 서버와 운영 체제가 작동 중인지 따위를 조사하는 응용 프로그램

### 2022.03.05 test

1과목 100점

2과목 75점

3과목 70점

4과목 65점

5과목 95점

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/37224b48-b482-4287-89e2-38479900f6d0/Untitled.png)

```cpp
22번 단위 테스트에서 테스트의 대상이 되는 하위 모듈을 호출하고,
파라미터를 전달하는 가상의 모듈로 상향식 테스트에 필요한 것
```

- 테스트 드라이버

드라이브는 상향식이기 때문에 “드라이브업”이라고 외우자

```cpp
34번 NS chart 거리가 먼 것
```

- 화살표나 GOTO를 사용해 이해하기 쉽다.

`NS chart` 는 Flow Chart 최대 단점인 `화살표가 표시되지 않는다.`

```cpp
36번 분할정복에 기반한 알고리즘으로 피벗을 사용하며
최악의 경우 n(n-1)/2 회의 비교를 수행해야 하는 알고리즘
```

- 퀵정렬

`피벗`을 사용하는것은 `퀵정렬`이다. 피자는 퀵 피벗은 퀵

```cpp
38번 소프트웨어 품질 관련 국제 표준인 ISO/IEC 25000에 관한 설명으로 옳지 않은 것
```

- ISO/IEC 2501n에서는 소프트웨어 내부 측정, 외부, 사용품질, 품질 측정 요소 등을 다룬다.

나오면 그냥 틀리자.

```cpp
39번 코드 인스펙션과 관련한 설명으로 틀린 것
```

- 동적 테스트 시에만 활용하는 기법이다.

`코드 인스펙션`은 `실행 시키는 것 대신 눈으로만 확인`, 품질 향상 기법 중 하나, `결함과 함께 코딩 표준 준수 여부, 효율성 등도 검사, 정적 테스트에 가까움`

```cpp
43번 어떤 릴레이션 R의 모든 조인 종속성의 만족이 R의
후보 키를 통해서만 만족될 때, 이 릴레이션 R이 해당하는 정규형
```

- 제5정규형

제1정규형 : 모든 속성 도메인이 원자값으로만 구성

제2정규형 : 부분 함수 종속 제거

제3정규형 : 이행 함수 종속 제거

BCNF : 결정자 함수 종속

제4정규형 : 다치 종속성 제거

5NF : 조인 종속성 제거

`“원부이 결다조”`

```cpp
44번 E-R 모델에서 다중값 속성 표기법
```

- 이중원

마름모 : 관계

사각형 : 개체

선 : 관계-속성 연결

```cpp
50번 A→B, B→C일 때 A→C인 관계를 제거하는 단계
```

- 2NF → 3NF

이행종속을 제거하는 과정

```cpp
51번 CREATE TABLE문에 포함되지 않는 기능
```

- 속성 타입 변경

그 외 NOT NULL여부 지정, 기본키 구성 속성 지정, CHECK 제약 조건 정의 등은 가능하다.

```cpp
57번 SQL 쿼리 결과문
```

- UNION은 합집합에 대한 의미로, 모두 합치면 된다.

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/faac0d26-833b-44f3-8788-a94ba3890dc0/Untitled.png)

```cpp
58번 분산 DB시스템에 대한 설명 중 틀린 것
```

- 분산 DB 시스템의 주요 구성 요소는 분산 처리기, P2P시스템, 단일 데이터베이스 등이 있다.

분산 데이터베이스 주요 구성 요소는 전역, 분할, 할당, 지역 스키마 등이 있으며, 논리적으로 하나의 시스템에 속하지만 물리적으로 여러  개의 컴퓨터 사이트에 분산되어 있는 시스템이다.

```cpp
61번 IP 주소체계와 관련한 설명으로 틀린 것
```

- IPv6의 패킷 헤더는 32 octet의 고정된 길이를 가진다.

IPv4의 헤더는 가변적, 주소크기는 32bit

IPv6의 헤더는 고정적, 주소크기는 128bit

```cpp
62번 C언어 관련
```

*이 무엇을 뜻하는지

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/ea6a9bed-eb86-4807-b038-e80d5e930ea6/Untitled.png)

```cpp
74번 빌드 도구에 해당되지 않는 것
```

- Kerberos

Kerberos는 컴퓨터 네트워크 인증 암호화 프로토콜이다.

빌드 도구로는 Ant, Maven, Gradle 등이 존재한다.

```cpp
76번 C언어 관련
```

*이 무엇을 뜻하는지

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/d6136aef-e9d5-4bc1-9d47-3e01024a3d8d/Untitled.png)

```cpp
78번 C언어 관련
```

<<이 무엇을 뜻하는지

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/cfc0a1e3-f415-4059-b6ba-6a315916d71d/Untitled.png)

```cpp
80번 쉘의 주요 기능에 대한 설명이 아닌 것은?
```

- 쉘 프로그램 실행을 위해 프로세스와 메모리를 관리한다.

```cpp
87번 블루투스 공격과 해당 공격에 대한 설명이 올바르게 연결된 것
```

- 블루프린팅 - 블루투스 공격 장치의 검색 활동을 의미

블루버그 : 블루투스 장비 사이 취약점 공격

블루스나프 : 블루투스 취약점을 활용해 파일에 접근해 정보 열람

블루재킹 : 스팸처럼 명함 퍼뜨려 “재킴”

## 22.03.05 해설영상 관련 - https://youtu.be/ecWXL1dbNts

---

```cpp
ip 주소체계 관련 추가
```

ip 변환 방법 `듀얼스택, 터널링, 변환`

ipv4 : 유니캐스트(1:1), 멀티캐스트(1:다), 브로드캐스트(무작위)

ipv6 : 유니캐스트, 멀티캐스트, `애니캐스트`

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/613af9a4-1690-4b9d-8ab3-79baeb416672/Untitled.png)

```cpp
tcp/ip 프로토콜
IP -> MAC으로 바꾸는 건 ARP
MAC -> IP로 바꾸는 건 RARP
프로토콜의 3요소 : 구문, 의미, 타이밍
정보보안의 3요소 : 기밀성, 무결성, 가용성
```

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/5a3c08d0-c4aa-4dd5-986f-7c0b3ec1c912/Untitled.png)

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/009a576c-994c-405a-8c58-8ba5b279f402/Untitled.png)

```cpp
c언어 관련 문제
```

str1은 100번지, str2는 200번지를 갖는다고 했을 때

p1은 100번지, p2는 200번지를 가리키는 포인터 변수가 된다.

str1[1]= 101번지인데, 101번지의 값을 p2[2]로 바꾼다는 것이기 때문에 p2의 V값을 str[1]인 O에 넣는다

따라서 str1 = KVREA가 되고 같은 원리로

str2 = LOVA가 된다.

마지막에 출력은 p1의 +2이므로 100번지 + 2해서 102번지 p1의 102번지 즉, 2번째 인덱스로 접근하면 R이 된다.(3번)

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/6180103d-e527-4534-a5f7-87cc53af2850/Untitled.png)

c언어 관련 : 좌시프트

1을 비트로 변환해서 1개를 왼쪽으로 민다

즉, 1 → 10이 되고 값은 2가 된다. 식들을 모두 계산하면 (2번)

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/694fdac7-99e5-443c-bacd-76756a9f4088/Untitled.png)

### 2021.08.14 test

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/c97f4160-2c38-4de0-85aa-10147e39ad03/Untitled.png)

---

```cpp
8번 클래스 동작, 클래스에 속하는 객체에 대해 적용될 메서드를 정의한 것이자,
UML에서는 동작에 대한 인터페이스를 지칭한 것은?
```

- Operation

동작, 적용될 메서드는 instance가 아닌 Operation이다

```cpp
15번 럼바우의 객체 지향 분석 기법 중 자료 흐름도를 주로 이용하는 것
```

- 기능 모델링

럼바우 `객 동상 가자`(료흐름도) 로 외우면된다.

럼바우 `‘정객 제동 가디’`

객체 모델링 : 정보 모델링, 시스템에서 요구

동적 모델링 : 제어, 흐름, 동작

기능 모델링 : DFD

```cpp
21번 테스트를 목적에 따라 분류했을 때 강도테스트에 대한 설명 옳은 것
```

- 시스템에 과다 정보를 부과하여 과부하 시 시스템이 정상 작동 하는지

말그대로 강도를 테스트 하기 위함이므로 시스템 과부하를 일으켜보는 테스트

```cpp
28번 제품 소프트웨어 사용자 매뉴얼 작성 절차
```

- 작성 지침 정의 - 사용 설명서 구성 요소 정의 - 구성 요소별 내용 작성 - 사용 설명서 검토

지침 정의 → 요소 정의 → 내용 작성 → 설명서 검토

```cpp
39번 테스트 케이스 자동 생성 도구를 이용해 테스트 데이터를 찾아내는 방법이 아닌 것
```

- 스텁과 드라이버

스텁과 드라이버는 통합테스트이다.

테스트 수행도구 : 자료 흐름도, 기능 테스트, 도메인 분석, 랜덤 테스트

```cpp
47번 관계 데이터베이스에서 관계 대수 연산이 아닌 것
```

- Fork

관계 대수 연산에는 select, project, join, division 등이 있다. 관계대수 셀프조디

```cpp
52번 관계 대수에 관해 틀린 것
```

- 원하는 릴레이션을 정의하는 방법을 제공하며 비절차적 언어이다.

관계대수란 관계형 데이터베이스에서 원하는 정보와 그 정보를 검색하기 위해 어떻게 유도하는가를 기술하는 절차적인 언어이다.

```cpp
55번 정규화에 대한 설명으로 틀린 것
```

- 개념적 설계 단계 이전에 수행한다.

정규화는 논리적 설계 단계에서 수행한다. 즉, 개념적 이후

```cpp
60번 Y는 X에 함수 종속이다의 표시
```

- X → Y

```cpp
61번 모듈 내 구성 요소가 서로 다른 기능을 같은 시간대에 함께 실행할 때 응집도
```

- Temporal Cohesion

시간적 응집도

```cpp
70번 C Class 속하는 Ip
```

- 200.168.30.1

0~128 A class 10000000

128~192 B class 11000000

192~224 C class 11100000

224~240 Dclass 11110000

```cpp
71번 C언어 실행 결과
```

- &를 붙이면 주소를 참조하기 때문에

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/05aa9e8e-79f2-4f88-9c2b-f40112aa3668/Untitled.png)

- 결론적으로 &n - *&pt를 0이며 나머지만 계산

```cpp
76번 TCP헤더와 관련한 내용으로 틀린 것
```

- 윈도우 크기는 송수신 측 버퍼 크기로 최대 32767bit이다

TCP 헤더에 있는 window size는 16비트로 65535이다.

그 외 맞는 것으로 순서번호는 전달하는 바이트마다 번호가 부여되며, 수신번호확인은 상대 호스트에서 받으려는 바이트의 번호를 정의, 체크섬은 데이터를 포함한 세그먼트의 오류를 검사한다.

```cpp
97번 시스템이 몇 대가 되어도 하나의 시스템에서 인증에 성공하면
다른 시스템에 대한 접근권한도 얻는 시스템은?
```

- SSO

Single Sign On의 줄임

### 2021.05.15 test

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/4ae0dd36-dbb8-4b9c-b3fa-8d381ade7d84/Untitled.png)

```cpp
1번 시스템의 구성요소로 볼 수 없는 것
```

- Maintenance

유지보수는 시스템 구성요소에 포함되지 않는다

```cpp
2번 유스케이스에 대한 설명 중 옳은 것
```

- 액터는 대상 시스템과 상호 작용하는 사람이나 다른 시스템에 의한 역할이다.

사용자 액터는 기능을 요구하는 대상이나 시스템의 수행결과는 통보받는 사용자 혹은 기능을 사용하게 될 대상

시스템 액터는 사용자 액터가 사용한 유스케이스를 처리해주는 외부 시스템

```cpp
8번 소프트웨어 아키텍쳐 설계에서 시스템 품질속성이 아닌 것
```

- 독립성

시스템 품질 속성은 가용성, 변경용이성, 보안성, 사용편이성, 시험용의성 등 6가지이다.

```cpp
9번 데이터베이스와 애플리케이션으로 부터 연계 테이블 또는
파일 형태로 생성하여 송신하는 시스템
```

- 송신 시스템

```cpp
14번 UML 다이어그램이 아닌 것
```

- 절차 다이어그램

UML 다이어그램 종류

1. 구조적 다이어그램 - 클래스, 객체, 컴포넌트, 배치, 복합체, 패키지 - “클객컴배복패”
2. 행위 다이어그램 - 유스케이스, 시퀀스, 커뮤니케이션, 상태, 활동, 타이밍, 상호작용 - “유시커상활타상”

```cpp
22번 다음 중 단위 테스트를 통해 발견할 수 있는 오류가 아닌 것
```

- 모듈 간 비정상적 상호작용으로 인한 원치 않는 결과

```cpp
24번 버전 관리 항목 중 저장소에 새로운 버전 파일로 갱신하는 것
```

- 체크인

형상 감사 : 기준선의 무결성을 평가하기 위해 확인, 검증, 검열 과정을 통해 공식적으로 승인하는 작업

```cpp
30번 블랙박스 테스트를 통해 발견할 수 있는 오류가 아닌 것
```

- 반복 조건을 만족하는데도 루프 내 문장이 수행되지 않는 경우

블랙박스 테스트 : 경계값 분석, 기능 테스트, 프로그램 구조 고려x

화이트 박스 테스트 : 문장검증, 선택검증, 경로검증, 조건검증

```cpp
35번 인터페이스 구현 검증 도구가 아닌 것
```

- Foxbase

STAF, watir, xUnit 등은 인터페이스 구현 검증 도구가 맞다.

```cpp
41번 병렬 데이터베이스 환경 중 수평 분할에 활용되는 기법이 아닌 것
```

- 예측 분할

```cpp
44번 SQL에서 VIEW를 삭제할 때 사용하는 명령어
```

- DROP

추가적으로 VIEW에서는 ALTER 사용이 불가능

```cpp
48번 릴레이션에서 기본 키를 구성하는 속성은 널값이나
중복 값을 가질 수 없다는 것을 의미하는 제약조건
```

- 개체 무결성

개체 무결성 : 기본키를 구성하는 속성은 널값이나 중복값을 가질 수 없다.

참조 무결성 : 외래키 값은 널이거나 참조 릴레이션의 기본키 값과 동일 해야함

도메인 무결성 : 특정 속성의 값이 그 속성이 정의된 도메인에 속한 값이어야 함

기밀성 : 데이터를 보지도 못하게

무결성 : 수정도 못하게

```cpp
49번 병행제어 기법의 종류가 아닌 것
```

- 시분할 기법

병행 제어는 동시 여러개 트랜잭션을 병행수행할 때 DB의 일관성을 파괴하지 않도록 제어하는 것

병행제어 기법 종류로는 로킹, 타임 스탬프, 최적 병행 수행, 다중 버전 등이 존재한다.

시분할 기법은 사용자가 여러 작업이 동시에 느껴지게 하는 것이다.

```cpp
61번 무선 랜에서 데이터 전송 시 매체가 비어 있음을 확인한 뒤
충돌을 피하기 위해 임의 시간을 기다린 후 데이터를 전송하는 방법
```

- CSMA/CA

CSMA/CA “충돌 감지” 충돌에 대비해 학인 신호를 전송

```cpp
62번 가장 약한 결합도는?
```

- Stamp Coupling

약해지는 순서로는

(내)게 (공부)하라고 강요하지 마요

(제)가 (스)트레스 받(자)나요 or 내공부 제스잘

내용(content) > 공통(common) > 외부(external) > 제어(control) > 스탬프(stamp) > 자료(data)

```cpp
66번 서브넷 마스크
```

- 잘 모르겠음

```cpp
67번 자바 실행 결과
```

- 어이없는거 틀림 y는 x그대로 받고, x증가, z는 x에서 1뺀거 받음

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/91683aa5-b488-456e-9d42-69049cbbd3a1/Untitled.png)

```cpp
68번 프로세스 적재 정책과 관련해서 틀린 것
```

- 일반적으로 페이지 교환에 보내는 시간보다 프로세스 수행에 보내는 시간이 더 크면 스레싱이 발생한다.

반대이다. 페이지 교황네 보내는 시간이 프로세스 수행 시간보다 크면 스레싱이 발생

```cpp
73번 c언어 실행 결과
```

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/4e8ba867-92c1-4727-83ab-359fcbb6cda4/Untitled.png)

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/bd70a888-62d3-48ef-8a5c-1a15a16c5b4a/Untitled.png)

```cpp
75번 가장 강한 응집도
```

- Sequential Cohesion

응집도의 순서는 강해지는 순서로

우연적(coincidental) > 논리적(logical) > 시간적(temporal) > 절차적(procedural) > 교환적(communication) > 순차적(sequantial) > 기능적(functional)

(우)리 (논)산 (시)(절) 기억나?

(교)자랑 (순)대 (기)대 했는데

```cpp
76번 TCP 프로토콜과 관련한 설명으로 틀린 것
```

- 인접한 노드 사이 프레임 전송 및 오류를 제어한다.

```cpp
78번 C언어 라이브러링 중 stdlib.h에 대한 설명으로 옳은 것
```

- 문자열을 수치 데이터로 바꾸는 문자 변환함수와 수치를 문자열로 바꿔주는 변환 함수 등이 있다.

stdlib.h 함수 → atoi, atof, itoa 등

stdio.h → 표준 입출력 라이브러리

strlen() → string.h 라이브러리

```cpp
81번 고속 전송과 장거리 연결 및 멀티 프로토콜 기능 활용,
각기 다른 운영체제의 여러 기종들이 데이터 공유, 여러 개의
저장장치나 백업 장비를 단일화 시킨 시스템
```

- SAN

MRP : memory buffer register, 데이터가 잠시 기억되는 레지스터

NAC : network access control, PC의 MAC주소를 IP관리 시스템에 등록, 일관된 보안관리

NIC : 물리 계층과 데이터 계층 서비스를 제공

```cpp
86번 OSPF(Open Shortest Path First)에 대한 설명으로 옳지 않은 것
```

- 거리 벡터 라우팅 프로토콜이라고 한다.

거리벡터 프로토콜 : 벨만포드 알고리즘, RIP(최대 15홉), 인접 라우터와 주기적 정보 교환

링크 프로토콜 : 다익스트라 알고리즘, OSPF 프로토콜(홉 제한 없음), 최단 경로

```cpp
88번 원래 12536으로 기입되어야 하는데 12936으로 표기된 경우 어떤 코드 오류인가
```

- Transcription Error

사본오류(Transcription Error) : 작업자나 광 문자 인식 프로그램에서 발생하는 데이터 입력 오류

```cpp
89번 ISO 12207 표준의 기본 생명주기의 주요 프로세스에 해당하지 않는 것
```

- 성능평가 프로세스

기본 생명주기 : 획득, 공급, 개발, 운영, 유지보수

지원 생명주기 : 품질 보증, 검증, 확인, 활동 검토, 문제해결

조직 생명주기 : 관리, 기반 구조, 훈련, 개선 프로세스

```cpp
93번 해시 기법에 대한 설명으로 틀린 것
```

- 주로 공개키 암호화 방식에서 키 생성을 사용한다.

해시는 단방향이고 공개키는 양방향으로 해당 지문이 틀린 것

```cpp
96번 침입 차단 시스템에서 외부 네트워크와 내부 네트워크 사이에 두는 완충적인 통신망은?
```

- Screened Subnet

### 21.03.07 test

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/8e56d72c-dac4-419a-ab80-a70407eb7c13/Untitled.png)

```cpp
7번 Coad-YourDon 방법에 해당하는 것
```

- E-R 다이어그램을 사용해 객체의 행위를 데이터 모델링하는데 초점을 둔 방법이다.

```cpp
11번 CASE(Coumputer Aided Software Engineering)에 대한 설명으로 틀린 것
```

- 소프트웨어 사용자들에게 사용 방법을 신속히 숙지시키기 위해 사용된다.

CASE 도구는 일관성 분석을 통해 요구사항 변경사항의 추적 및 분석, 관리, 표준 준수여부 등을 확인한다.

```cpp
12번 UML에 대한 설명 중 틀린 것
```

- State Diagram은 객체들 사이 메시지 교환을 나타내며, Sequence Diagram은 하나의 객체가 가진 상태와 그 상태의 변화에 의한 동작순서를 나타낸다.

State Diagram은 객체가 가진 상태와 상태 변화를 나타낸다.

Sequence Diagram은 객체 사이에 오가는 메시지를 시간순으로 나타낸다.

```cpp
14번 다음 중 요구사항 모델링에 활용되지 않는 것
```

- 단계 다이어그램

애자일 방법, 유스케잇 다이어그램, 시퀀스 다이어그램 등은 요구사항 모델링에 쓰인다.

```cpp
19번 UML 다이어그램 중 시스템 내 클래스의 정적 구조를 표현,
클래스와 클래스, 클래스의 속성 관계를 나타내는 것
```

- Class Diagram

```cpp
22번 선택 정렬을 사용해 오름차순으로 정렬한 경우 PASS 3의 결과
```

- 선택 정렬은 작은애를 선택해 정렬되지 않는 첫 번째 요소와 바꾼다.

```cpp
27번 디지털 저작권 관리에 사용되는 기술요소가 아닌 것
```

- 방화벽

디지털 저작권 관리 기술요소 : 암호화, 키관리, 암호화 파일생성, 식별 기술, 저작권 표현, 정책관리, 크랙방지, 인증

```cpp
30번 자료구조에 대한 설명으로 틀린 것
```

- 큐는 비선형구조에 해당한다.

리스트, 스택, 큐 → 선형

트리, 그래프 → 비선형

```cpp
33번 소프트웨어 품질목표 중 쉽게 배우고 사용할 수 있는 정도를 나타내는 것
```

- Usability(유용성)

```cpp
37번 퀵 정렬에 관한 옳은 설명
```

- 레코드의 많은 자료 이동을 없애고 하나의 파일을 부분적으로 나누어 가면서 정렬한다.

```cpp
38번 해싱 함수의 종류가 아닌 것
```

- 개방 주소법

```cpp
45번 트랜잭션 전과 후 상태가 같아야한다.
```

- 일관성(consistency)

원자성 : 하나라도 오류가 있다면 트랜잭션이 원복되어야한다.

일관성 : 트랜잭션 전후가 일관적이어야한다.

격리성 : 트랜잭션이 격리적으로 수행되어야 한다.

영속성 : 트랜잭션의 결과는 지속성이 있어야한다.

```cpp
51번 그리스 문자 시그마를 사용하는 관계대수 연산
```

- Select

셀렉트 - 시그마

프로젝트 - 파이

조인 - 나비넥타이

division - 나누기

```cpp
54번 관계 데이터 모델에서 릴레이션에 관한 설명으로 옳은 것
```

- 도메인은 하나의 속성을 가질 수 있는 같은 타입의 모든 값을 집합으로 각 속성의 도메인은 원자값을 갖는다.

```cpp
55번 3NF에서 BCNF가 되기 위한 조건
```

- 결정자이면서 후보키가 아닌 것 제거

원부 이걸다줘?

```cpp
68번 TCP가 해당하는 계층
```

- 트랜스포트 계층(전송 계층)

```cpp
72번 운영체제의 가상기억장치 관리에서 프로세스가
일정 시간동안 자주 참조하는 페이지들의 집합
```

- Workgin Set

```cpp
73번 결합도가 낮은 순서대로
```

결합은 `내공부 재료 숙자`

내부 - 공통 - 외부 - 제어 - 스탬프 - 자료

```cpp
74번 가상기억장치의 고정된 크기 일정한 블록은 (   )기법과
가변적 크기 블록 (    ) 기법이 있다.
```

- Paging, Segmentation

```cpp
81번 다음 암호 알고리즘 중 성격이 다른 하나
```

- AES

MD4, MD5, SHA-1은 해시 암호화 알고리즘

AES는 대칭키 암호화 알고리즘

```cpp
82번 크래커가 침입해 백도어를 만들어 놓거나 설정 파일 변경 분석 도구
```

- tripwire

```cpp
83번 경량메시지 전송 프로토콜, IBM 주도
```

- MQTT

```cpp
89번 블록체인 개발환경을 클라우드로 서비스하는 개념,
블록체인 네트워크에 노드 추가 및 제거가 용이
```

- Baas

```cpp
92번 고품질 전력서비스, 에너지 이용효율 극대화 전력망
```

- 스마트 그리드

```cpp
94번 스트림 암호화 방식의 설명으로 옳지 않은 것
```

- 해쉬 함수를 이용한 해쉬 암호화 방식을 사용한다.

해시함수는 단방향 암호화로, 양 방향 암호화인 스트림 방식과 다름

비트, 바이트, 단어들을 순차적으로 암호화한다, RC4는 스트림 암호화 방식에 해당, 대칭키 암호화 방식이다

```cpp
100번 공개키 암호에 대한 설명으로 틀린 것
```

- 10명이 공개키 암호를 사용할 경우 5개의 키가 필요하다.

비밀키는 N(n-1)/2 개의 키가 필요하고 공캐키는 2n개가 필요하다.

즉 비밀키인 경우 10명이면 45개가 필요하고 공개키는 20개가 필요하다.